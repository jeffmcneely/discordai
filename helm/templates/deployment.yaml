apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "discord-bot.fullname" . }}
  labels:
    {{- include "discord-bot.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "discord-bot.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "discord-bot.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "discord-bot.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            # Discord Bot Configuration
            {{- if .Values.env.DISCORD_BOT_TOKEN }}
            - name: DISCORD_BOT_TOKEN
              value: {{ .Values.env.DISCORD_BOT_TOKEN | quote }}
            {{- end }}
            
            # OpenAI Configuration
            {{- if .Values.env.OPENAI_API_KEY }}
            - name: OPENAI_API_KEY
              value: {{ .Values.env.OPENAI_API_KEY | quote }}
            {{- end }}
            {{- if .Values.env.OPENAI_MODEL }}
            - name: OPENAI_MODEL
              value: {{ .Values.env.OPENAI_MODEL | quote }}
            {{- end }}
            {{- if .Values.env.OPENAI_MAX_TOKENS }}
            - name: OPENAI_MAX_TOKENS
              value: {{ .Values.env.OPENAI_MAX_TOKENS | quote }}
            {{- end }}
            {{- if .Values.env.OPENAI_TEMPERATURE }}
            - name: OPENAI_TEMPERATURE
              value: {{ .Values.env.OPENAI_TEMPERATURE | quote }}
            {{- end }}
            {{- if .Values.env.OPENAI_INTEGRATION_ENABLED }}
            - name: OPENAI_INTEGRATION_ENABLED
              value: {{ .Values.env.OPENAI_INTEGRATION_ENABLED | quote }}
            {{- end }}
            
            # Rate Limiting Configuration
            {{- if .Values.env.RATE_LIMIT_MESSAGES_PER_MINUTE }}
            - name: RATE_LIMIT_MESSAGES_PER_MINUTE
              value: {{ .Values.env.RATE_LIMIT_MESSAGES_PER_MINUTE | quote }}
            {{- end }}
            {{- if .Values.env.RATE_LIMIT_TOKENS_PER_HOUR }}
            - name: RATE_LIMIT_TOKENS_PER_HOUR
              value: {{ .Values.env.RATE_LIMIT_TOKENS_PER_HOUR | quote }}
            {{- end }}
            
            # Database Configuration
            {{- if .Values.env.DISCORD_DB_URL }}
            - name: DISCORD_DB_URL
              value: {{ .Values.env.DISCORD_DB_URL | quote }}
            {{- end }}
            
            # Application Configuration
            - name: LOG_LEVEL
              value: {{ .Values.discordBot.logLevel | quote }}
            - name: MAX_MESSAGES_PER_MINUTE
              value: {{ .Values.discordBot.maxMessagesPerMinute | quote }}
            {{- if .Values.redis.enabled }}
            - name: REDIS_URL
              value: "redis://{{ include "discord-bot.fullname" . }}-redis-master:6379"
            {{- end }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            exec:
              command:
                - python
                - -c
                - "import discord; print('Bot OK')"
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            exec:
              command:
                - python
                - -c
                - "import discord; print('Bot OK')"
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: tmp-volume
              mountPath: /tmp
            - name: logs-volume
              mountPath: /app/logs
      volumes:
        - name: tmp-volume
          emptyDir: {}
        - name: logs-volume
          emptyDir: {}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
