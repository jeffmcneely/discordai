apiVersion: v1
kind: Secret
metadata:
  name: discordai
  namespace: default  # Change to your target namespace
type: Opaque
data:
  # Base64 encoded values - replace with your actual base64 encoded credentials
  # To encode: echo -n "your-value" | base64
  DISCORD_BOT_TOKEN: ""           # Your Discord bot token (base64 encoded)

---
# Alternative: StringData version (Kubernetes will auto-encode)
# apiVersion: v1
# kind: Secret
# metadata:
#   name: discordai
#   namespace: default
# type: Opaque
# stringData:
#   DISCORD_BOT_TOKEN: "your-discord-bot-token"
#   OPENAI_API_KEY: "your-openai-api-key"
#   OPENAI_MODEL: "gpt-4"
#   OPENAI_MAX_TOKENS: "500"
#   OPENAI_TEMPERATURE: "0.7"
#   OPENAI_INTEGRATION_ENABLED: "true"
#   RATE_LIMIT_MESSAGES_PER_MINUTE: "5"
#   RATE_LIMIT_TOKENS_PER_HOUR: "10000"
